---
layout: page
title: delete
subtitle: delete
categories: []
tags: []
menus:
    - mapper
    - method
    - delete
author: likly
date: 2019-10-15 14:21:33 +800
version: 1.0
---

# Delete

## Methods

### Origin

```java
/**
 * 删除符合条件的数据并返回影响的行数
 *
 * @param tableName 表名
 * @param ids       IDS
 * @param query     条件
 * @return 删除符合条件的数据所影响的行数
 */
int delete(@Param("tableName") String tableName, @Param("ids") Collection<ID> ids, @Param("query") Query query);
```

### Override

```java
Query int delete(T... entities);

Query int delete(String tableName, T... entities);

Query int delete(List<T> entities);

Query int delete(String tableName, List<T> entities);

Query int delete(ID... ids);

Query int delete(String tableName, ID... ids);

Query int delete(Collection<ID> ids);

Query int delete(String tableName, Collection<ID> ids);

Query int delete(Queryable query);

Query int delete(Query query);

Query int delete(String tableName, Queryable query);

Query int delete(String tableName, Query query);
```

## Mapper

```xml
<!--==============================================================================================================-->
<!--=====DELETE==============================GENERATED BY FINAL FRAMEWORK==============================DELETE=====-->
<!--==============================================================================================================-->
<delete id="delete">
    <trim prefix="DELETE FROM">
        <include refid="sql-table"/>
        <choose>
            <when test="ids != null">
                <include refid="sql-where-ids"/>
            </when>
            <when test="query != null">
                <include refid="sql-query"/>
            </when>
        </choose>
    </trim>
</delete>
```

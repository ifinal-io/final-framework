---
layout: page
title: Insert
subtitle: 满足你的日常插入操作
description: 简单的数据Insert操作，支持分表、批量插入
categories: []
tags: []
menus:
    - mapper
    - method
    - insert
author: likly
date: 2019-10-15 13:07:15 +800
version: 1.0
---


# Insert

## Methods

```java
int insert(T... entities);

int insert(Collection<T> entities);

int insert(String tableName, T... entities);

int insert(String tableName, Collection<T> entities);

int insert(Class<?> view, T... entities);

int insert(Class<?> view, Collection<T> entities);

int insert(String tableName, Class<?> view, T... entities);

int insert(String tableName, Class<?> view, Collection<T> entities);

int insert(Collection<T> entities, Queryable query);

int insert(Collection<T> entities, Query query);

int insert(String tableName, Collection<T> entities, Queryable query);

int insert(String tableName, Collection<T> entities, Query query);

int insert(Class<?> view, Collection<T> entities, Queryable query);

int insert(Class<?> view, Collection<T> entities, Query query);
```

## Sql

```xml
<!--==============================================================================================================-->
<!--=====INSERT==============================GENERATED BY FINAL FRAMEWORK==============================INSERT=====-->
<!--==============================================================================================================-->
    <insert id="insert">
        <selectKey keyProperty="id" order="BEFORE" resultType="java.lang.String">
            <trim prefix="SELECT" suffix="FROM dual">MD5(REPLACE(UUID(), '-', ''))</trim>
        </selectKey>
        <trim prefix="INSERT INTO">
            <include refid="sql-table"/>
        </trim>
        <choose>
            <when test="view == null">(columns)</when>
        </choose>
        <trim prefix="VALUES">
            <foreach collection="list" index="index" item="item" separator=",">
                (item.properties...)
            </foreach>
        </trim>
        <include refid="sql-query"/>
    </insert>
```
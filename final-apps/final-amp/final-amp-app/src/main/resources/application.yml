spring:
  datasource:
    driver-class-name: org.h2.Driver
    schema: classpath*:db/schema.sql
    url: jdbc:h2:mem:test
    username: sa
    password: sa
  h2:
    console:
      enabled: true

logging:
  level:
    org.ifinal.finalframework: debug
dubbo:
  registry:
    address: zookeeper://localhost:2181
  application:
    name: amp
